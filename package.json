{
  "name": "context-memory",
  "version": "3.1.1",
  "description": "Regenerates the active index in a Context directory's AGENTS.md, validates its frontmatter, and says when a consolidation pass is due.",
  "type": "module",
  "bin": {
    "context-memory": "scripts/index.mjs"
  },
  "files": [
    "scripts/index.mjs"
  ]
}
